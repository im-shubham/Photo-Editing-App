pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url "https://jitpack.io" }
        jcenter()
        mavenCentral()
        flatDir {

            dirs 'libs'

        }
    }
}
rootProject.name = "Photo Editing App"
include ':app'
